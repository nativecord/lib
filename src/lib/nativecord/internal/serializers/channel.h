#pragma once

#include "nativecord/internal/objectbase.h"

namespace nativecord
{
    NC_JSON_DECLFUNCS(struct ThreadMetadata);
    NC_JSON_DECLFUNCS(struct ThreadMember);

    NC_JSON_DECLFUNCS(struct DefaultReaction);
    NC_JSON_DECLFUNCS(struct Overwrite);

    NC_JSON_DECLFUNCS(class Channel);
}
